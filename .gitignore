# Secrets — never commit
.streamlit/secrets.toml

# Database (local only — Streamlit Cloud uses ephemeral storage)
data/signals.db

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Logs
logs/

# Backtest outputs (large, not needed for the app)
backtest results/

# Local research / experiment scripts (not part of the deployed app)
experiments/

# Nexus bot runtime state (poll offset — machine-local)
.nexus_bot_offset

# Stray junk
=

# One-time migration scripts
migrate_to_supabase.py

# Claude Code local config
.claude/

# Claude auto-memory (internal to Claude Code sessions)
memory/

# OS
.DS_Store
